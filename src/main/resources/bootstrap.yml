spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configServer
      profile: dev
  application:
    name: eurekaClient
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
    enabled: true